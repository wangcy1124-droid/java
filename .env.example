# Copy to .env and set a local development password. Never commit .env.
MYSQL_PASSWORD=change-me-local-only

RABBITMQ_USER=hmdp
RABBITMQ_PASSWORD=change-me-local-only

# Server Compose overrides (optional)
WEB_BIND=127.0.0.1
WEB_PORT=8080
APP_JAVA_OPTIONS=-Xms512m -Xmx512m -XX:+UseG1GC
